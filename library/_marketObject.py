class Iteration():
    def __init__(self,count=0):
        self.current_iteration = count
        # self.current_market = marketHistory(current_iteration)

    def iterate(self):
        # read news table for current_iteration
        # sum veracity > 1 per country
        # Top two countries go to war
        # iteration +=1
        # new_market(supply, country)-=1
        # new_market(demand,country) = current_market(demand,country) + current_market(future_demand,country)
        # new_market(future_demand,country)-=1
        # new_market(conflict,country) = true
        # marketHistory.update(iteration,new_market)
        # return new_market()
        return 0
